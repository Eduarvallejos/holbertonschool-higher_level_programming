// Wait for the DOM content to be fully loaded before executing the code
$(document).ready(function () {
  // Select the <header> element using the jQuery API and update its text color to red
  $('header').css('color', '#FF0000');
});
